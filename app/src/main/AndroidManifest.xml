<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- No neverForLocation: some OEM Bluetooth stacks need ACCESS_FINE_LOCATION for full scan results anyway. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".FlipWidgetApplication"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.FlipVehicleWidget">

        <activity
            android:name=".presentation.auth.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.FlipVehicleWidget">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          tools:node="replace" wipes out every attribute of AppAuth's own declaration, not just
          the intent-filter this override exists to change: android:theme must be repeated
          here too, or this activity (an AppCompatActivity) crashes with "You need to use a
          Theme.AppCompat theme (or descendant) with this activity" the moment a real redirect
          reaches it, since it'd otherwise fall back to this app's plain (non-AppCompat) default
          theme.
        -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Translucent.NoTitleBar"
            tools:node="replace">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${oauthRedirectHost}"
                    android:pathPrefix="/oauth/callback" />
            </intent-filter>
        </activity>

        <!--
          AppAuth-Android's own AuthorizationManagementActivity (a separate component from
          RedirectUriReceiverActivity above; this one relays the completed redirect back to
          MainActivity's ActivityResult callback) ships with launchMode="singleTask". On
          Android 14+ that causes it to be recreated instead of resumed when the redirect
          arrives, silently losing the authorization result: MainActivity just restarts with
          isAuthenticated still false, no crash, no error, token exchange never even attempted.
          Overriding to singleInstance is the confirmed fix:
          https://github.com/openid/AppAuth-Android/issues/977
        -->
        <activity
            android:name="net.openid.appauth.AuthorizationManagementActivity"
            android:exported="false"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboard|keyboardHidden"
            android:theme="@style/Theme.AppCompat.Translucent.NoTitleBar"
            android:launchMode="singleInstance"
            tools:node="replace" />

        <receiver
            android:name=".presentation.widget.VehicleWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/vehicle_widget_info" />
            <meta-data
                android:name="com.samsung.android.appwidget.provider"
                android:resource="@xml/samsung_vehicle_widget_info" />
        </receiver>

        <receiver
            android:name=".presentation.widget.WidgetActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.flipvehiclewidget.app.action.EXECUTE_COMMAND" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".presentation.widget.WidgetConnectReceiver"
            android:exported="false" />

        <!-- Target of the system-managed BLE scan's PendingIntent; see VehicleBeaconScanner. -->
        <receiver
            android:name=".presentation.widget.VehicleBeaconReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.flipvehiclewidget.app.action.BEACON_SCAN_RESULT" />
            </intent-filter>
        </receiver>

        <!-- Re-registers the BLE scan after reboot, since scan registrations don't survive one. -->
        <receiver
            android:name=".presentation.widget.VehicleBeaconBootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>
</manifest>
